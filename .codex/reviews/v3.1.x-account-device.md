# Codex review - v3.1.1..v3.1.3 account-device restructure

Verdict: APPROVE_WITH_NITS (no BLOCKER, no MAJOR)

## MINORs
1. account_number dedup ignores missing refs: multi-property with
   ["1234...", ""] still exposes a number. Show only for a single-property
   account (or require all refs equal + non-empty).
2. Comment/README claim email/phone/title attributes "limit PII in history"
   - inaccurate; HA records attributes. Reword (or disable-by-default).
3. Legacy per-property refresh button cleanup is snapshot-gated; on a
   no-cache failed bootstrap it lingers until a later successful reload.

## NITs
- Trailing blank line at EOF in binary_sensor.py.
- Stale update_problem entries left in strings.json + en.json; stale
  "Last update status" comment in const.py.

## Confirmed sound
- Hub via_device + pre-registration in __init__ before platform setup.
- Reload idempotent (async_get_or_create); entry removal clears hub.
- Button always added + always-available retained.
- Deprecation unique_id/platform targeting correct.
